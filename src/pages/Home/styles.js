import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  list: {
    height: 45,
    width: '90%',
    alignItems: 'center',
  },
});

export default styles;
